{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://steerable.dev/spec/blocks/AskUserQuestionsPayload.schema.json",
  "title": "AskUserQuestionsPayload",
  "description": "Payload of an ask-user-questions card. The agent has paused mid-run and needs structured input from the user before continuing. Each question is one of: option buttons (select), free-form text, or password. After the user answers, the backend re-emits the same card with answers filled in to switch the UI to read-only.",
  "type": "object",
  "additionalProperties": true,
  "required": ["intro", "questions"],
  "properties": {
    "intro": { "type": "string" },
    "outro": { "type": ["string", "null"] },
    "answers": {
      "type": ["object", "null"],
      "additionalProperties": {
        "oneOf": [
          { "type": "string" },
          { "type": "array", "items": { "type": "string" } }
        ]
      }
    },
    "questions": {
      "type": "array",
      "minItems": 1,
      "maxItems": 4,
      "items": {
        "type": "object",
        "additionalProperties": true,
        "required": ["id", "text", "multiSelect"],
        "properties": {
          "id": { "type": "string" },
          "text": { "type": "string" },
          "header": {
            "type": "string",
            "maxLength": 12,
            "description": "Short chip label shown above the question (Claude Code AskUserQuestion parity). Optional; hosts derive one from `text` when absent."
          },
          "type": { "type": "string", "enum": ["select", "text", "password"], "default": "select" },
          "options": {
            "type": "array",
            "items": { "type": "string" },
            "minItems": 2,
            "maxItems": 4,
            "description": "Choices for a select question (2-4, CC parity). The host auto-appends an 'Other' free-text escape; the model does not list it."
          },
          "placeholder": { "type": ["string", "null"] },
          "multiSelect": {
            "type": "boolean",
            "description": "Whether the user may pick several options. Required (CC parity): the model commits to single vs multi explicitly."
          }
        }
      }
    }
  }
}
