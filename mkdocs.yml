site_name: Steerable Framework
site_description: Spec-first agent framework for TypeScript + Python.
site_url: https://pathlyapp.github.io/steerable-framework/
repo_url: https://github.com/pathlyapp/steerable-framework
repo_name: pathlyapp/steerable-framework
edit_uri: edit/main/docs/

theme:
  name: material
  features:
    - navigation.instant
    - navigation.tracking
    - navigation.sections
    - navigation.expand
    - navigation.indexes
    - navigation.top
    - content.code.copy
    - content.code.annotate
    - content.tabs.link
    - toc.follow
    - search.suggest
    - search.share
  palette:
    - media: "(prefers-color-scheme: light)"
      scheme: default
      primary: indigo
      accent: indigo
      toggle:
        icon: material/brightness-7
        name: Switch to dark mode
    - media: "(prefers-color-scheme: dark)"
      scheme: slate
      primary: indigo
      accent: indigo
      toggle:
        icon: material/brightness-4
        name: Switch to light mode

markdown_extensions:
  - admonition
  - attr_list
  - def_list
  - footnotes
  - tables
  - toc:
      permalink: true
  - pymdownx.details
  - pymdownx.highlight:
      anchor_linenums: true
  - pymdownx.inlinehilite
  - pymdownx.snippets
  # `pymdownx.superfences` powers fenced code blocks. The `custom_fences`
  # entry routes ```mermaid``` blocks through Material for MkDocs' built-in
  # Mermaid integration so diagrams render as SVG instead of as preformatted
  # text (which depends on box-drawing glyphs being present in the user's
  # font and on no horizontal-wrap CSS being applied).
  - pymdownx.superfences:
      custom_fences:
        - name: mermaid
          class: mermaid
          format: !!python/name:pymdownx.superfences.fence_code_format
  - pymdownx.tabbed:
      alternate_style: true

nav:
  - Home: index.md
  - Getting Started: getting-started.md
  - Spec:
      - Overview: spec/overview.md
      - Architecture: spec/architecture.md
      - Events: spec/events.md
      - Tools: spec/tools.md
      - Chat: spec/chat.md
      - Safety: spec/safety.md
      - Runtime: spec/runtime.md
      - Sidecar: spec/sidecar.md
  - UI Components: ui/index.md
  - Migration:
      - From DeepPath: migration/deeppath.md

plugins:
  - search

extra:
  social:
    - icon: fontawesome/brands/github
      link: https://github.com/pathlyapp/steerable-framework
